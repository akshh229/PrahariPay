// Crypto Service
// Handles key generation and signing

export const generateKeys = async () => {
    // Generate keypair
};

export const signTransaction = async (tx, privateKey) => {
    // Sign tx
};
