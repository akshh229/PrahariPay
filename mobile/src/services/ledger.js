// Ledger Service
// Handles local storage of transactions

export const saveTransaction = async (tx) => {
    // Save to AsyncStorage/SQLite
};

export const getLedger = async () => {
    // Get all transactions
    return [];
};
